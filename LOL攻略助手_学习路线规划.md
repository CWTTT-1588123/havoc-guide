# LOL 攻略助手 —— 个人学习路线规划

> 面向：23岁应届生 / 延边大学 211 本科 / 人工智能专业 / 0实习 / 偏内向 · 爱钻研 · 执行力强 / 高薪高压倾向
> 目标：打造一个有竞争力的简历项目（LOL 攻略助手，AI应用方向），并据此积累技能、拿到浙江/深圳的 AI 相关 offer。
> 学习原则（根据你的性格定制）：吃「内在动机」，不吃「外部强制」→ 用**项目驱动**学习，不要给自己定"每天必须学X小时"这种会放弃的计划。

---

## 一、终极目标

做一个**能部署上线、且面试能讲清楚**的 AI 应用作品：

```
一个「英雄联盟攻略助手」：
  用户问 → 助手基于【最新版本数据 + 玩家攻略文本】给出准确回答（出装、连招、版本强势英雄、符文搭配……）
```

它不是"只会调 API"，而是能**检索你自己的知识库并生成回答**（RAG），并且**能通过网页访问**（FastAPI）、**能挂到服务器上**（Linux + Docker）。

**一句话：** 用这个项目，证明你具备「AI应用落地」能力，覆盖雇主对 AI应用/开发岗的核心要求。

---

## 二、核心技术栈总览（先记住这个"两个壳"的框架）

```
┌────────────────────────────────────────────────┐
│  ①「AI内核」： 大模型API + RAG（手写）(+ LangChain)  │  ← 决定"聪明不聪明"
│  ②「工程外壳」： FastAPI + Linux + Docker          │  ← 决定"能不能被人用起来"
└────────────────────────────────────────────────┘
```

- ① 是项目的**灵魂**，① 没跑通项目就是空壳。
- ② 是**锦上添花**，让项目从"笔记本里的代码"变成"别人能访问的产品"，面试加分。

**核心优先级：** ① 必学必做；② 先做 FastAPI，Linux/Deploy 放最后补。

---

## 三、学习路线（分阶段，带时间预估和交付物）

> 下面的"交付物"就是你**每一阶段完成时要拿得出的东西**，最终它们会**拼成你的简历作品**。每阶段做完，都要能跟人讲清楚"我做了什么、为什么这么设计"。

### 阶段 0：Python 基础（约 1 个月）
**目标：** 能独立写脚本、会用常用库，为后续打地基。
- [ ] 语法：变量、列表/字典、函数、类、模块
- [ ] 常用库：`requests`（发请求）、`json`（处理数据）、`os`/`pathlib`（文件）
- [ ] 会读写 JSON/CSV 文件
- [ ] 会用 `pip` 装包、用 `git` 提交代码（git 必须学，简历要写）

**交付物：** 能写一个小脚本（比如抓取一个网页的 JSON 并保存成文件）。

### 阶段 1：大模型 API（约 1 周）
**目标：** 明白"API = 租用大模型"，能调用一个模型返回结果。
- [ ] 注册一个模型服务，拿 API Key（推荐 **DeepSeek** 或 通义/智谱，国内好用）
- [ ] 用 `requests` 写一个 `POST` 调用，问一个问题拿到回答
- [ ] 理解「messages」结构（system/user/assistant）

**交付物：** 一个能问答的 python 脚本（小例子见文末附录1）。

### 阶段 2：数据采集（约 1-2 周）
**目标：** 拿到"英雄/装备/技能"的**结构化数据**（骨架）+ 一部分**攻略文本**（精髓）。
- [ ] 用 **Data Dragon**（Riot 官方免费数据，无 key）抓英雄数据
      - 版本号：`https://ddragon.leagueoflegends.com/api/versions.json`
      - 中文英雄：`https://ddragon.leagueoflegends.com/cdn/{版本号}/data/zh_CN/champion.json`
      - 装备 `items.json`、符文 `runesReforged.json`、召唤师技能 `summoner.json`
- [ ] 把抓到的数据存成 JSON/CSV，**固定一个版本号**（标注清楚，防止后期"过时"）
- [ ] （可选，加分）抓少量第三方攻略文本（B站/知乎/NGA）做爬虫，或**自己整理/写**攻略文本
  - ⚠️ 爬虫的坑：反爬、限速、版权。**建议先自己写/策展内容**，跑通后再考虑爬虫。

**交付物：** 一个由 Data Dragon 抓好的结构化数据文件 + 若干篇攻略文本。

### 阶段 3：手写 RAG（约 2 周）【最核心，必学】
**目标：** 弄懂并亲手实现"检索增强生成"，这是项目灵魂、也是面试重点。
- [ ] 把攻略文本**切片**（chunking）
- [ ] 用嵌入模型把每个切片转成**向量**，存进向量库（推荐 **Chroma** 或 **FAISS**，入门友好）
- [ ] 提问 → 问句转向量 → 检索最相似几段
- [ ] 把检索到的段落**拼进 prompt**，发给大模型 API，让它基于资料回答
- [ ] 加一条"找不到就说不知道"的兜底逻辑（防幻觉）

**交付物：** 一个能回答"这版本谁强势/某英雄怎么出装"的 RAG 脚本。**这是你项目的核心 Demo。**

### 阶段 4：FastAPI（约 1 周）
**目标：** 让 RAG 能被网页调用，变成可访问的接口。
- [ ] 用 FastAPI 写一个 `POST /ask` 接口，接住用户问题
- [ ] 让它返回你的 RAG 结果
- [ ] 本地跑起来：`uvicorn main:app --reload`，用浏览器/工具测试

**交付物：** 一个能通过网址访问的接口（能演示给面试官看）。

### 阶段 5：Linux + Docker 部署（约 1-2 周）【加分，放最后】
**目标：** 把项目打包、部署到一台云服务器，让别人能网上访问。
- [ ] Linux 基础：`cd`/`ls`/`cat`/`pip`/`ssh`/`sudo`（会几个常用命令即可，不用当运维）
- [ ] 写一个 `Dockerfile` 把应用+依赖打包（见文末附录2）
- [ ] 买一台云服务器（阿里云/腾讯云学生机，跑 Linux）
- [ ] 在服务器上 `docker run` 跑起容器，绑定端口，公网可访问

**交付物：** 一个部署在云服务器上、**别人能访问**的在线助手。

### 阶段 6：打磨 & 求职（贯穿）
- [ ] 把项目写在简历上（用 STAR 结构讲清楚：做了什么、技术栈、难点、效果）
- [ ] 放上 GitHub，写好 README（说明架构、截图/演示）
- [ ] 海投浙江/深圳的 AI应用/开发/数据岗，先拿 offer 练手
- [ ] 建议今年 12 月补考**六级**（部分大厂是筛选条件）

---

## 四、那这些技能到底对应什么岗位？（重要）

你练的这个项目，正好覆盖下面这些岗位的高频要求，**学的东西 100% 对得上职位**：

| 你学到的东西 | 对应岗位要求 |
|---|---|
| Python | 几乎所有 AI/开发岗 |
| 大模型 API + RAG | AI应用工程师、大模型应用开发、算法（应用方向） |
| 向量库 / 数据采集 | 数据处理、AI工程 |
| FastAPI | 后端/全栈、AI后端 |
| Linux + Docker | 工程化、部署、AI工程岗加分项 |

**求职定位建议：** 别冲最卷的"算法研究员"（要研究生/论文/竞赛），走**AI应用开发 / 大模型应用 / 数据**这条更实际、量大、简历对得上的路。

---

## 五、学习与心法（针对你的性格，避免"三分钟热度"）

1. **项目驱动，别"先学再做"。** 每阶段都奔着"做出一个看得见的东西"去，而不是纯啃概念。
2. **别一次性啃全部。** 你容易对强制性的每日计划放弃，所以**按阶段做**，每完成一个阶段就停下来收获成就感，再往下走。
3. **允许"半成品"，先让东西"活"。** 做出一个能跑的 Demo 比什么都强；爬虫、Docker 这些坑先放一放，别一上来就被卡住。
4. **把爱好变资产。** 你 LOL 玩了七八年，本身就是优势——把对游戏的理解写进攻略、做成项目，它就是差异化竞争力。（但要分清：**创作的**打游戏是资产，**纯消遣的**沉迷是消耗。）
5. **先找靶子再学。** 去招聘软件搜目标岗位，看 JD 里的技能词，那就是你的学习清单。别漫无目的地学。

---

## 六、注意事项 / 坑（提前避开）

- **版本过时问题**：LOL 每个版本都更新。Demo 阶段**固定一个版本**的数据即可，先跑通，后期再加"定时刷新"。
- **版权/合规**：用 **Riot 官方 API + Data Dragon** 完全合法。大规模爬 B站/知乎/掌盟可能违反平台条款甚至法规，**学习/简历项目小规模、用于个人学习**通常没事，但别做成公开盈利成品还大量爬取。稳的位置：**优先官方 API + 自己策展内容**。
- **别被框架过度封装**：LangChain 不是必须的，且学习成本高。**建议先手写 RAG 理解原理**，再决定要不要用框架。
- **预期管理**：0 实习 + 刚毕业，**很难一上来就拿高薪**。短期目标 = "先进门、拿第一份 AI offer、攒 2-3 年硬经验"，高薪是之后的事。心态调对了才不焦虑。

---

## 附录1：大模型 API 最简调用示例（示意）

```python
import requests

resp = requests.post(
    "https://api.deepseek.com/chat/completions",
    headers={"Authorization": "Bearer 你的Key"},
    json={
        "model": "deepseek-chat",
        "messages": [{"role": "user", "content": "卡莎这版本怎么出装？(DeepSeek 默认输出内容)"}],
    },
)
print(resp.json()["choices"][0]["message"]["content"])
```

## 附录2：最简 Dockerfile（示意）

```dockerfile
FROM python:3.11
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

---

## 附：学习时间总览（参考，非死板）

| 阶段 | 内容 | 预估时间 | 是否必须 |
|---|---|---|---|
| 0 | Python 基础 | 1 月 | ✅ 必须 |
| 1 | 大模型 API | 1 周 | ✅ 必须 |
| 2 | 数据采集 (Data Dragon) | 1-2 周 | ✅ 必须 |
| 3 | 手写 RAG | 2 周 | ✅ 必须（核心） |
| 4 | FastAPI | 1 周 | ✅ 重要 |
| 5 | Linux + Docker 部署 | 1-2 周 | ⭐ 加分 |
| 6 | 打磨&求职 | 贯穿 | —  |

> 强调：**阶段0-4 是"生死线"，先做完**。阶段5 的 Linux/Docker 是"锦上添花"，等项目 demo 成型后再补。
