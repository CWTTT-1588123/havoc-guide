# 海克斯大乱斗 攻略站 —— 项目总览（ds_brain）

> 这是整个项目的"大脑"：一次看懂的**概览、架构、当前进度、快速上手**。
> 更新：本文件每次工作后应同步更新"当前进度"。

## 文档目录（按需阅读）
| 文件 | 内容 |
|---|---|
| `00_总览_README.md` | 本项目：概览/架构/进度/快速上手/文件清单/注意事项 |
| `01_全过程路线.md` | **完整过程步骤**（起源→需求→抓包→直连→BFS→清洗→统计→符文名→网站→后续） |
| `02_数据获取_抓包与爬虫.md` | mitmproxy 探路 → curl_cffi 直连 → BFS玩家图谱爬虫（含关键突破/错误处理/凭证刷新） |
| `03_数据管线与统计口径.md` | extract/metadata/stats 详细逻辑 + 统计口径（Wilson置信/组合/协同/克制/出装/排除A类） |
| `04_网站功能与前端.md` | FastAPI 后端 + 前端单页（首页/详情/详见页/品质徽标/小字说明/箭头） |
| `05_运行手册与当前进度.md` | 常用命令/备份/系统注意/常见问题（下次接手先看这个） |
| `06_记忆记录模板_给agent的话术.md` | **一键话术**：想记录"why/route、进度、做了什么"时，复制对应模板给 agent 即可，不用重新打字 |
| `07_需求与技术现状.md` | **需求/技术债**：功能清单、技术栈、问题(SHA-256/demo验证码/无测试类型框架)、明确不做(LLM/RAG/DL)、改进路线 |
| `08_常见错误与解决.md` | **⚠️ 错误日志**：项目所有踩坑+解决办法（部署/登录/前端/统计/测试/git），**新错误务必追加这里** |
| `09_GitHub从零到回滚.md` | **Git/GitHub 新手教程**：从零上传 → 每次改动再上传 → **怎么回滚**（reset/revert 区别），含全命令速查 |
| `10_AI聊天需求.md` | **🤖 AI 聊天智能体需求**：点 Q 版人物开聊、站点专属 AI 答疑（英雄/符文/装备/胜率+闲聊）、技术方案与分步实施计划 |
| `11_移动端兼容需求.md` | **📱 移动端兼容需求**：现状盘点 + 12 项需求清单 + CSS 媒体查询适配计划（✅ v1.3 已实施并部署，双端差异化） |
| `12_微信小程序上架方案.md` | **📱 微信小程序**：havoc-mp 工程（uni-app/Vue3）上架方案（源码在 `havoc-mp/`，已提交 git） |
| `13_爬虫唤醒话术.md` | **🔁 爬虫一键唤醒**：新会话直接复制整段话给 AI，即可恢复爬虫全过程（状态检查/换凭证/滑块/落袋/规律与坑） |
| `14_小程序注册填报文案.md` | **📝 小程序注册**：微信小程序注册/类目/备案等填报文案（配合 `12_微信小程序上架方案`） |

---

## 一、项目是什么
一个面向 LOL **「海克斯大乱斗」**（内部模式名 `KIWI`，`queue=2400`）的**英雄攻略站**：
- **主页**：英雄胜率排行榜（按胜率排序，可搜索/正倒序），每行显示该英雄**前3最优符文**。
- **英雄详情页**：整体胜率、**最优单个符文**（按白银/黄金/棱彩分档）、**最优符文组合**、**搭配增益**、**核心出装**、**克制推荐**。
- 目标：让玩家**最快最直接**得到"这英雄该带什么符文/怎么打"。

核心**判定标准 = 胜率**（来自爬取的国服真实对局数据）。

---

## 二、技术栈 / 架构

```
[WeGame 抓包(mitmproxy, 一次性探路)] → [直连爬虫(curl_cffi 伪装Chrome) BFS玩家图谱]
        ↓ 海克斯大乱斗对局JSON
[extract.py 清洗] → [metadata.py 元数据] → [stats.py 统计(Wilson置信)] → [stats.json]
        ↓
[FastAPI webapp.py] → [前端 static/index.html] → 网站(http://127.0.0.1:8000)
```

**依赖已分离（各自独立 venv）**：
- **爬虫 venv** `wegame-capture\.venv`：`curl_cffi`（伪装 Chrome TLS/HTTP2 指纹，直连 WeGame 接口，核心）+ `mitmproxy`（抓包）。清单见 `wegame-capture\requirements.txt`。
- **网站 venv** `havoc_guide\.venv`：`fastapi + uvicorn`（后端）、`pypinyin`、`python-multipart`、`bcrypt`。清单见 `havoc_guide\requirements.txt`。
- （备用）**league-tools**：可解析游戏 WAD（未实际用于符文名）；**requests/标准库**：DDragon 元数据。

---

## 三、当前进度（务必同步）
| 项 | 状态 |
|---|---|
| **限定版本** | **16.17**（分析/网站只统计这个补丁；`extract`+`bfscrawl` 都加了版本过滤，默认16.17，systemd 单元也是 16.17） |
| 分析对局数(16.17) | **本地已落袋 97,662 场 / 976,620 条**（2026-09-10 深夜；线上 84,397 场，**待部署 +13,265 场**） |
| 原始爬取文件(各版本) | data/ ~**11.0 万** 个（仍在增长） |
| 玩家池 | **31 万+**（双账号四线 BFS 扩展；黑色玫瑰暂停） |
| 英雄 | 172 个 |
| 符文 | 208 个 distinct（进统计 202） |
| 符文名/品质 | ✅ 真实（来自 Kiwi 表） |
| 英雄标签(对位用) | ✅ 全标（DDragon自动+人工补充） |
| 网站 | ✅ **已上线 `https://haikelol.com`**（HTTPS+域名，Nginx 反代；备用 `http://8.137.175.107:8000`） |
| **域名** | ✅ `haikelol.com` 已购+已实名；✅ **ICP 备案已完成**（拿到备案号）——可正式上线 |
| **登录/认证** | ✅ 注册(仅邮箱)/密码登录/邮箱验证码注册/忘记密码(邮箱真发)/绑定手机号登录；角色体系(主管理员/管理员/普通用户)+管理后台 |
| 管理员 | **admin@qq.com / admin123**（主管理员 owner；首个注册的账户） |
| `/api/version` | 返回 `16.17`；`/api/site` 返回 `game_version` + `site_version(v1.3.2)` + ICP/公安备案号 |
| **收信箱/公告** | ✅ 个人中心信封图标（名字/邮箱右侧，未读红色角标）→ 点击弹窗：**左侧版本列表 + 右侧版本内容**；选中未读即已读；公告按版本存 `announcements` 表（`ver` 列），每用户已读存 `users.read_anns`；管理后台「公告管理」标签发布（版本号+标题+内容） |
| **AI 人设自定义** | ✅ 个人中心「编辑 AI 助手」写自定义提示词（存 `users.ai_prompt`，聊天时覆盖默认人设，男专业/女活泼） |
| **评论/统计** | ✅ 评论区敏感词过滤（`sensitive_words.txt`）；管理后台「访问统计」：总/今日/近14天/页面分布 + **桌面/手机分端统计**（`views.mobile` 列按 UA 判断）；管理员已登录设备自动排除（前端跳过+后端校验双重保险） |
| 主数据文件 | `havoc_guide\processed\stats.json`（16.17） |
| **技术现状/债** | 密码 hash=**bcrypt**（旧SHA-256已自动升级）；验证码**安全流**（服务端生成/只发邮箱/不回传/60s限频/用后即删）；**有 pytest、有类型标注(pydantic+mypy)**；**前端 Vue 3 + Vite（`web/`，组件化，已部署）**；**无大模型/RAG/深度学习** |

**线上运行状态**：
- **正式站**：阿里云 ECS 上 `systemd havoc-guide.service` 常驻（单进程，`--workers 1`），无需后台任务。
- **本地开发站**：需手动 `uvicorn`（后台任务易断）；**爬虫已恢复**：策略=**只爬艾欧尼亚双线**（a1_w1+a1_w2，0.3s），黑色玫瑰暂停；凭证 ~1.5h 过期，用**一键换凭证脚本** `recap_on.py`/`recap_off.py` 抓新凭证（脚本不拉爬虫，爬虫由 AI 用托管后台任务拉起）。

**最近改动（见 ds_done 日志）**：
- **上线部署**：纯Python + systemd 部署到阿里云 ECS，公网可访问（关键坑：`--host 0.0.0.0`、`--workers 1`）。
- **登录/认证系统**：真邮件验证码(163 SMTP)/注册(仅邮箱)/忘记密码/绑定手机号登录/角色体系(主管理员/管理员/普通)+管理后台。
- **技术栈正规化（安全+可靠性）✅**：① 密码 **bcrypt**（旧SHA-256首登自动升级）；② **验证码安全流**（服务端生成/只发邮箱/不回传前端/60s限频/用后即删）；③ **pytest**（27个测试：统计6+接口13+模型8）；④ **pydantic 请求模型 + 类型注解 + mypy**。
- **git/GitHub 备份 ✅ 完整**：仓库已建（Private，GitHub: CWTTT-1588123/havoc-guide），`captured/` 抓包凭证等敏感文件已正确 gitignore（踩坑：gitignore 注释不可放在模式同行）。→ **已 push main 成功并全量同步**（踩坑：Windows schannel 报 SEC_E_NO_CREDENTIALS → `git config http.sslBackend openssl`；国内 443 → 代理 `git config --global http.proxy http://127.0.0.1:7897`；Fine-grained 令牌按仓库授权 → 推别的仓库报 403 需单独令牌）。
- **版本标记 v1.0**：把 `havoc-guide` 正式版（提交 `5c868de`）打 tag `v1.0` 并推上 GitHub，可建对应 **Release** 发布页（教程见 `09_GitHub从零到回滚.md`）。
- **Git 教学/练习**：用户全程学会 建库→add/commit→三种 reset→理解 .git 结构→push 上云→每次改动再 push→打版本 tag；练习仓库 `git-practice`（本地 `git练习/`）。
- **环境分离**：爬虫与网站**各建独立 venv**（爬虫 `wegame-capture\.venv` 只装 curl_cffi+mitmproxy；网站 `havoc_guide\.venv` 装 fastapi 全家桶），并清理了爬虫 venv 里混装的网站包。
- **前端**：登录弹窗白底加宽、验证码注册(含确认密码)、密码框睁/闭眼SVG、dark模式对比度、头像菜单不透明、自定义favicon。
- **前端 Vue 3 组件化 ✅（2026-09-04）**：`havoc_guide/web/`（Vite+SFC），全部页面组件化并与原版**一比一对齐**（CSS 逐字复制原版 style；登录/个人中心/后台/排行榜/详情+评论区/分类/所有符文/桌宠/悬浮提示/外号搜索/自定义背景截取预览）；后端加评论接口 + response_model。
- **🤖 AI 聊天智能体 ✅（2026-09-05 上线 v1.1）**：点 Q 版人物开聊；「小海克斯」专属人设（男=专业/女=活泼）；DeepSeek + 检索注入（英雄/装备外号+真实胜率）+ 多轮记忆 + 登录可用 + 频率限制。**v1.1 已推 git 并部署到服务器**（`/` 改为服务 `web/dist` 构建产物）。
- **v1.2（2026-09-05）**：AI 聊天**流式回复**（SSE 逐字输出，Nginx `proxy_buffering off`）；**SEO**（robots.txt/sitemap.xml/`/champ/{id}` 预渲染页/hash 深链接）；百度站长验证文件路由。
- **v1.3（2026-09-05）**：**移动端适配 + 双端差异化 UI** —— 手机端：排行榜排名徽章（1金2银3铜）/三个推荐符文固定三排/详情页单个符文两列+排名序号/品质标签小字/符文名与品质两行/点符文卡居中弹窗（点外部关闭）/克制板块两行/个人中心三行竖排；**电脑端保持原样**（胜率场次两列、卡片并排、悬浮提示）。另：顶栏标题/头像加大、Q宠锁定 ✓ 蓝圈+记忆修复、背景预览弹窗（电脑横框/手机竖框）。
- **v1.3.1（2026-09-05）**：个人中心**换绑邮箱**（向新邮箱发验证码→验证后新邮箱成为登录邮箱，`/api/auth/email_code`+`/api/auth/email`）；未登录点「登录后开始聊天」先关聊天窗只留登录页；**手机端登录两步流**（点登录/注册先弹「登录后可以」弹层 → 弹层底部「登录/注册」按钮 → 登录页，电脑端悬浮+直开登录页不变）。
- **数据大更新 + 多区爬取体系（2026-09-06/07）**：16.16 → **16.17** 全量切换；破解新版**滑块验证**（查他人战绩前需过滑块，账号级开关）；实测风控边界（同 cookie 安全档=3进程×0.3s，窗口 25~40 分钟/次验证，cookie 寿命 ~1.5h）；双区并行（艾欧尼亚+黑色玫瑰）BFS 爬取，16.17 累计 **4.3 万场/42.7 万条**（头部英雄 6,200+ 场）；`watch_crawl.py` 爬虫监视器（弹窗提醒滑块/凭证/饱和）。
- **备案号上线（2026-09-07）**：✅ 公安备案审批通过，页脚展示 **蜀ICP备2026051382号-1 + 川公网安备51172402000199号**（`/api/site` 增加 `police` 字段，链接工信部/公安备案平台）。
- **评论管理 + 手机端后台入口（2026-09-07）**：管理后台新增**评论管理**（管理员查看全部评论+删除，`/api/admin/comments`+`/api/admin/comment/{id}/delete`）；手机端无法打开悬浮头像菜单 → **个人中心新增「管理后台」按钮**（仅管理员可见）。
- **v1.3.2（2026-09-08，git tag 已推）**：① **收信箱/公告系统**：个人中心头像旁**信封图标**（未读红色角标）→ 点击弹窗（**左侧版本列表 + 右侧版本内容**，选中未读即已读）；后端 `announcements` 表（`ver` 版本号列）+ `users.read_anns` 已读列，首播 v1.3.2/v1.3.1 两条公告；管理后台第 4 标签「公告管理」发布（版本号+标题+内容，全站用户收信箱可见）；**登录不弹窗**。② **AI 人设自定义**：个人中心「编辑 AI 助手」自定义提示词（`users.ai_prompt`，聊天覆盖默认人设）；Q 版形象改**单选**。③ 评论区**敏感词过滤**（`sensitive_words.txt`）+ 管理后台「访问统计」（排除管理员浏览）；备案双号页脚。④ 16.17 数据扩充至 **50,518 场/50.5 万条**。⑤ 微信小程序 `havoc-mp` 首版源码提交。**事故教训**：公告表加 `ver` 列漏写迁移 → 后端启动崩溃 502，已修复并加保护（见 08）。
- **手机端修复（2026-09-08/09）**：① 更换邮箱验证码行、公告标题行溢出 → 手机端改上下堆叠 + `input` 强制 `width:100%`（input 默认 20 字符固有宽会冲出容器）；② 管理后台重置密码原来用 `window.prompt`——**手机浏览器不弹输入框** → 改自制站内弹窗；③ 个人中心「管理后台」按钮移至「我的账号」标题上方；④ AI 人设输入框删除示例文案改中性提示。
- **访问统计设备区分（2026-09-09）**：`views` 表加 `mobile` 列（按 User-Agent 判断手机/桌面）；管理后台 4 卡展示（总/今日/桌面(含今日)/手机(含今日)）；管理员排除加**双重保险**（前端 is_admin 直接不打点 + 后端验身份 + `/api/pv` 整体 try 防崩）。
- **爬取重启 + 一键换凭证（2026-09-08/09）**：凭证过期 → 重抓包恢复爬取；策略改为**只爬艾欧尼亚双线**；新增 **`recap_on.py`/`recap_off.py` 一键换凭证脚本**（用户自己 PowerShell 跑：搭环境→点对局→清理+验证+自动合成凭证，全程中文提示；爬虫由 AI 托管后台任务拉起）；`test_api.py` 改为自动取最新抓包；**9/9 落袋 62,195 场/62.2 万条已部署线上**（备份 `stats_backup_20260909.json`），本地继续积累 ~17,100 场未落袋。
- **待做**：把本地 90,037 场（stats.json）scp 部署上线（+5,640 场）；百度站长平台提交 sitemap.xml 加快收录；凭证过期就 recap 换证 + `start_crawl.ps1` 续爬（窗口随验证次数递减，隔夜恢复）。
- **爬虫效率优化 + 瓶颈定位（2026-09-10）**：① `bfscrawl.py` 新增 **`visited_<区>_w<n>.json`**（持久化"已抓过"玩家）→ 队列只排**未抓过的玩家**（新发现玩家 LIFO 插队），停止前保存状态（滑块/凭证过期也保存，实测生效）；② 去重集合改**从文件名提取** game_id（10 万文件 **0.4s**，旧实现 28s~4min）；③ **实测关键数据**：随机玩家前 6 页未抓率 **95~100%**、每玩家可产 **44~52 场**、每请求产出 **~0.86 场**（已近最优）→ 真实瓶颈是**请求延迟 ~0.95s + DELAY 0.3s**（2 线 ≈ 80 场/分钟），提速应走"降 DELAY / 加线数"而非改去重；④ 教训：**爬虫运行期间用同一凭证做探测会提前触发滑块**（当晚 20:15 实测），已记 08。
- **双账号四线实测成功（2026-09-10 晚 21:00）**：把爬虫从"单账号 2 线"升级为"**双账号各 2 线**"（`start_crawl.ps1` 支持 Worker 1~4，各带自己的 `-CredFile`；3/4 号线玩家池预建）。实测 **~80 场/分钟 → ~155~170 场/分钟（2.1×）**，每账号仍 1.6 req/s 不增风控压力；**账号 A 在 21:10 触发滑块时只停了它的 W1/W2，账号 B 的 W3/W4 照常运行**（故障只降级一半）。实验前提：两个凭证的 `id`/`uin` 必须不同（开爬前先比对，防止同号叠加）。
- **爬取流程加固（2026-09-09 晚）**：新增 **`start_crawl.ps1` 唯一拉起入口**（AI 托管后台任务跑两条 `-Worker 1/2`）；`recap_off.py` 修复 `kill_mit` 拼写错误并**改为只抓凭证+验证、不再自动拉爬虫**（分离进程弹窗被关→爬虫静默死，详见 08）；监视器 `watch_crawl.py` 退役，改由任务完成通知+日志尾部判断停止；`13_爬虫唤醒话术` 重写为权威唤醒入口；**当晚 19:43 凭证跑满 W1=2,450 + W2=2,575 ≈ 5,000 场，21:07 过期收工**。
- **组合胜率口径修复（2026-09-09 深夜）**：发现"最优符文组合"整列 100%（整套符文精确集合几乎不重复，2场小样本霸榜）→ 改为**任意 3 符文子集**统计（≥20 场，收缩+Wilson 排序），前端/AI 展示**收缩点估计 est**（贝叶斯后验均值，好组合高于英雄平均；排序仍用 wilson 下界防霸榜）；stats.py/webapp.py/前端已改、`stats.json` 已重算（待部署），详见 08/03。

---

## 四、文件清单（都在工作区）

**`E:\Deepseek Harness\`**
| 文件/目录 | 作用 |
|---|---|
| `LOL攻略助手_学习路线规划.md` | 最初的学习规划（个人） |
| `LOL海克斯大乱斗攻略站_需求文档.md` | 需求文档（已定稿） |
| `ds_brain\` | **本项目文档（当前所在）** |

**`E:\Deepseek Harness\wegame-capture\`（数据抓取）**
| 文件/目录 | 作用 |
|---|---|
| `bfscrawl.py` | **主爬虫**（BFS玩家图谱，抓满每玩家历史） |
| `start_crawl.ps1` | **拉起爬虫唯一入口**（AI 托管后台任务跑：自动取最新凭证+设参数+重定向日志） |
| `recap_on.py` / `recap_off.py` | **一键换凭证**（用户自己 PowerShell 跑；只抓凭证+验证，不拉爬虫） |
| `test_api.py` | 凭证自测（自动取最新抓包，测 1/14/17 三区） |
| `crawler.py` | 单历史爬虫（备用/早期） |
| `addon.py` | mitmproxy插件（捕获 WeGame 接口） |
| `run_mitmweb.py` | 启动 mitmweb（抓包用） |
| `.venv\` | **爬虫虚拟环境**（curl_cffi/mitmproxy；网站另有 `havoc_guide\.venv` 装 fastapi 全家桶） |
| `data\` | **爬到的对局**（`detail_*.json`，~9971个） |
| `captured\` | 抓包原始文件 + **凭证来源**（`REQ_GetBattleDetail_*.json`） |
| `players_seen.json` | 已见过的玩家 openid |
| `hidden_players.json` | 隐藏/受限玩家记录 |

**`E:\Deepseek Harness\havoc_guide\`（处理+网站）**
| 文件/目录 | 作用 |
|---|---|
| `extract.py` | 原始JSON→结构化玩家记录（records.jsonl） |
| `metadata.py` | 英雄/装备名（DDragon） |
| `stats.py` | 统计引擎（胜率/组合/协同/克制/出装，Wilson置信） |
| `webapp.py` | FastAPI 后端（API + 服务前端 + **登录/认证/管理后台**） |
| `static/index.html` | 前端单页（首页+英雄详情+详见页+登录弹窗） |
| `processed\` | 输出：records.jsonl、stats.json、champion_names.json、item_names.json、augment_names.json、champion_tags.json、completed_items.json(成装ID,判定非小件)、**users.db(SQLite用户表, 主)**、users.json(旧表/迁移备份) |
| `deploy\setup.sh` | 服务器一键部署脚本（systemd，免交互） |
| `deploy/nginx.conf`、`deploy/nginx-haikelol.conf` | Nginx 反代 80/443→8000 模板（haikelol.com 版） |
| `Dockerfile`、`docker-compose.yml`、`requirements.txt`、`.env.example` | 容器部署 + 依赖 + SMTP/备案号环境变量样例 |
| `havoc_deploy.tar.gz` | 部署包（webapp/static/processed/start脚本；**排除36MB records.jsonl**） |

**`E:\英雄联盟符文id\kiwi\augments.json`**：从游戏文件提取的**符文ID→中文名/品质/描述**表（222个）——**符文名的权威来源**。

---

## 五、快速上手（下次怎么用）

### 看网站
```
& "E:\Deepseek Harness\havoc_guide\.venv\Scripts\python.exe" -m uvicorn webapp:app --host 127.0.0.1 --port 8000
# 在目录 E:\Deepseek Harness\havoc_guide 下运行；浏览器开 http://127.0.0.1:8000
```

### 抓更多数据（扩大）
```
# AI 在托管后台任务里跑（run_in_background + workdir=E:\Deepseek Harness\wegame-capture）：
& "E:\Deepseek Harness\wegame-capture\start_crawl.ps1" -Worker 1
& "E:\Deepseek Harness\wegame-capture\start_crawl.ps1" -Worker 2
# 脚本自动取最新凭证+设参数（16.17/双线/0.3s/低页数），输出到 crawl_a1_w1.log / crawl_a1_w2.log。
# 直连接口，不需要代理。凭证过期先让用户 recap_on/off 换证（详见 13_爬虫唤醒话术）。
```

### 刷新网站数据（爬取变多后）
```
# 数据管线属于网站项目，用网站 venv
& "E:\Deepseek Harness\havoc_guide\.venv\Scripts\python.exe" "E:\Deepseek Harness\havoc_guide\extract.py"
& "E:\Deepseek Harness\havoc_guide\.venv\Scripts\python.exe" "E:\Deepseek Harness\havoc_guide\stats.py"
# 然后重启 webapp 加载新的 stats.json
```

---

## 六、关键前提 / 注意事项
- **爬虫直连，不需要系统代理/证书**。它读 `captured\REQ_GetBattleDetail_*.json`（**最新**那份，`files[-1]`）里的 cookie/token 作为鉴权，用 `curl_cffi` 伪装 Chrome。
- **如果爬虫报 `8025009`（凭证过期）**：需要用 mitmproxy 重新抓一次 WeGame 的流量拿新 cookie（见 `02_数据获取_抓包与爬虫.md`）。
- **系统代理应关闭**（`ProxyEnable=0`），否则上网会被劫持。mitmproxy 证书只在"重新抓包"时才需要临时信任。
- 只爬 `queue=2400`（海克斯大乱斗）；隐藏玩家（`error 8000021`）自动跳过。
