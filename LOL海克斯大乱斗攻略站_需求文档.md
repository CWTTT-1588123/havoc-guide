# LOL 海克斯大乱斗 攻略站 —— 需求文档

> 版本：v0.3（已吸收你的全部反馈，接近定稿）
> 定位：面向 LOL「海克斯大乱斗」（**常驻模式**）的英雄攻略 + 强化符文推荐 + 出装推荐 + 冷知识的查询工具。
> 核心原则：娱乐模式，**不做复杂检索问答**，追求"快速、准确、直观"展示玩家想知道的东西——即"这个英雄该选什么符文"，以**胜率**判定。出装同理。
> 已否决：~~RAG 自然语言搜索~~。
> 本模式：**无段位区分**（简化，少一个分析维度）。

---

## 一、项目定位

- 用户：海克斯大乱斗玩家。
- 一句话核心诉求：**我玩某英雄，该带哪些符文、怎么搭配、这套阵容怎么针对性选、这个流派怎么出装**，答案主要**由胜率判定**。
- 形态：主页排行 + 英雄详情 + 出装推荐 + 对位/冷知识，**快、准、直观**，结论和胜率直接呈现。

---

## 二、模式机制（已确认）

- 单线对决（类大乱斗）。
- **强化符文选择节点**：**3 / 7 / 11 / 15 级**各一次，共 **4 次**选择。
- 每次选择**从 3 个选项挑 1**；**可刷新一次**，最多看到 **6 个选项**（首3 + 刷新后3）。
- 符文上限：最多 **5 个**强化符文。
- **哪一次能选 2 个：随机**（4 次中仅 1 次能拿 2 个，其余各拿 1 个）。
- **各选择点符文池不固定**（不一定只在某等级出现）。
- **品质（白银/黄金/棱彩）随机出现**，可能出现在任何回合，可能**多个棱彩**。
- **特殊"赌一把"符文**：把当前所有符文**随机变成任意棱彩**。
- **常驻模式。**
- **无段位区分。**

---

## 三、核心数据需求

每条对局记录应包含（含出装）：

| 字段 | 说明 | 用途 |
|---|---|---|
| 英雄 | 玩家所选英雄 | 主维度 |
| 符文选择 | 每个选择点选的符文（含品质/等级/回合） | 单符文 & 组合胜率 |
| 是否含"赌一把"符文 | 影响最终构筑随机性 | 统计特殊处理 |
| 出装 | 最终 6 件装备（核心三件套 + 后期两件） | 出装推荐 |
| 敌方阵容 | 对面 5 英雄 | 对位/针对推荐 |
| 己方阵容 | 己方英雄（可选） | 辅助分析 |
| 胜负 | 本局 win/loss | 胜率计算 |
| 版本 | 补丁版本 | 版本隔离 |
| 时间 | 对局时间戳 | 每日更新/趋势 |
| ~~段位~~ | ~~（本模式无段位，已去掉）~~ | — |

---

## 四、统计口径

### 4.1 单个符文胜率
- `WR(R|X) = 对局(X且含R且胜) / 对局(X且含R)`（出现即计入）。
- 可选严谨版：同回合内 R vs 其他候选的增益，判断"真实贡献"。

### 4.2 单个符文"推荐/优先级"排序
- **不能纯按胜率排**（小样本高胜率冲顶）。需**最小样本线 + 置信区间/贝叶斯收缩**，打分 `score = f(胜率, 样本量, 登场率)`，**标注样本量**。
- 核心交付：**"这英雄该选什么符文"按胜率可信地排好。**

### 4.3 综合符文（组合）胜率
- 精确组合胜率 `WR(组合|X)`；但因组合爆炸、样本少，用 4.4 协同增益兜底。

### 4.4 配合/协同
- **协同增益 = `WR(组合) − 预期WR(由各部件单符文胜率推算)`**。高出越多 = 真配合；样本不足用部件数据兜底。

### 4.5 出装胜率（**重点，处理符文干扰**）
- **核心原则：装备胜率只在「同一英雄 + 同一流派」内部比较，不跨流派比。**
  - 因为**流派已打包"符文背景 + 装备组合"**，锁定流派后符文背景基本一致，看到的差异主要是**装备本身的差异**。
- 定义：`WR(装备组合|X, 该流派)`；来源为"该流派核心三件套"的胜率/登场率。
- **诚实说明**：符文干扰能被流派分组**大体吸收**，但不能消除干净。娱乐模式够用即可；残余干扰 + "赌一把"随机性**标注**即可，不做因果级分析。

### 4.6 对位推荐 & 冷知识
- **对位条件胜率**：`WR(R|X, 对面属于类型T) − WR(R|X, 全部)`，超阈值才推荐。
- 例：对面有召唤物英雄（如马尔扎哈）→ 推荐"单位死亡掉落属性增强器"符文。
- 需**敌方阵容打类型标签**（见 5.3 的 7 类），**人工定义标签 + 程序算条件胜率**。
- 冷知识两类：① 数据驱动（自动生成，如"对召唤物阵容 +8%"）；② 人工整理（机制/相互作用，纯内容）。

### 4.7 "赌一把"符文特殊处理
- 会把所有符文**随机变成任意棱彩** → 最终构筑不稳定、噪声大。
- **处理**：含它的对局**单独标记**；统计/推荐时**默认剔除或单独分类**，或**显著标注"随机"**，避免污染稳定组合胜率。它本身可按"作为一种选择的胜率/登场率"单独看。

### 4.8 置信度 / 样本量（必做）
- 所有胜率统一**最小样本截断 + 置信区间**；高胜率但样本少 → **标注"样本不足"**。

---

## 五、功能需求

### 5.1 主页（排行榜）
- 顶部**搜索栏**（搜英雄）。
- **英雄胜率排行榜**（可**正序/倒序**切换）。
- 每行：**英雄头像 + 英雄胜率 + 该英雄 3 个最优符文**。
- **筛选指标：登场率**（用户已确认加入）。

### 5.2 英雄详情页
1. **英雄概览**：英雄类型、一般怎么玩、技能加点顺序（召唤师技能/加点）。
2. **单个符文最优（按胜率排序）**："带这个符文一定不会错、胜率最高"。
3. **符文组合最优（按胜率排序）**：用协同增益 + 样本量。
4. **对位/针对性推荐（冷知识）**：如"对面脆皮多 → 穿甲""对面有召唤物 → 属性掉落器"，配胜率数据（如"对召唤物阵容 +8%"）。

### 5.3 出装推荐（**确认纳入 MVP**）
- **核心三件套**：按**流派分类**给出核心 3 件装备。
- **玩家已圈定的 7 类流派**：**刺客 / 坦克 / AP / AD暴击 / 特效 / 攻速 / 辅助**。
- **剩下两件**：按战况选**输出**或**防御**两个方向分支。
- **呈现：把装备列出来即可，一般比较固定**（核心三件套较固定；后两件给"输出/防御"分支）。
- **胜率口径**：在**同一英雄+同一流派**内部比较核心三件套（见 4.5），后续补"该流派三件套胜率/登场率"支撑。

### 5.4 全局
- 搜索、排序、筛选（**登场率** / 版本，待定；**无段位筛选**）。
- 展示符文**品质（白银/黄金/棱彩）**标识。

---

## 六、数据采集（重要，需爬虫）

- 你确认 WeGame 里能看到海克斯对局的符文选择，**【数据有希望拿到，但必须用爬虫批量处理，不能一个一个看】**。
- 来源候选：① WeGame/腾讯系接口（首选）；② Riot 官方 API（兜底验证）；③ 第三方聚合（掌盟/OP.GG 等）。
- **第一步：小型可行性试点（爬虫）**——写一个爬虫抓一小批"该模式 + 英雄 + 符文 + 出装 + 胜负"数据，确认**能拿到哪些字段、覆盖率如何、麻烦程度**（敌方阵容/版本/时间能拿到多少）。
- **合规**：注意平台条款/反爬；学习/个人项目小规模通常可接受，但需评估，别大规模公开盈利。
- **每日/定时更新**：限速、增量更新、版本更新后数据作废；可先手动/半自动，后期做每日任务。

---

## 七、统计 & 算法 & 后端（模块划分）

```
[数据接入层(可插拔)] → [清洗/标准化] → [统计引擎(胜率/协同/对位/出装)]
        ↓                                       ↓
   [存储/数据库] ← [版本管理+每日更新]     [推荐逻辑(打分排序)]
                                              ↓
                                   [FastAPI 后端接口] → [前端网页]
```

- 数据接入层统一接口，数据源可换（WeGame/官方/第三方）。
- 统计引擎：单符文/组合/对位/出装胜率 + 置信度/样本量；**赌一把符文单独处理**；**出装按流派分组**。
- 推荐逻辑：符文、组合、对位、出装打分排序。
- 存储：SQL（英雄/对局/出装/符文）；冷知识走结构化内容（**不需要向量库/RAG**）。
- 版本管理 + 每日更新（异步任务）。

---

## 八、非功能需求 / 注意事项

- **性能**：排行/详情要快（索引/缓存）；每日更新别阻塞前端。
- **合规**：数据源、爬取需评估。
- **可维护性**：符文池/装备随版本变化，需**动态维护符文/装备清单及品质/描述/流派**。
- **可读性**：所有推荐**给胜率 + 样本量**，别只给干巴巴结论。
- **"赌一把"随机性**：界面/统计上**明确标识**。

---

## 九、已定的关键决策（汇总）

| 决策点 | 已定 |
|---|---|
| 模式 | 常驻、无段位 |
| 符文机制 | 4次选择/3选1/可刷新/随机一次选2/池不固定/品质随机/有"赌一把" |
| 搜索问答（RAG） | **不做** |
| 出装推荐 | **纳入 MVP**，核心三件套 + 流派 + 后两件输出/防御 |
| 流派分类 | 刺客 / 坦克 / AP / AD暴击 / 特效 / 攻速 / 辅助（7类） |
| 敌方阵容标签 | 召唤物 / 脆皮 / 坦克 / 刺客 / 治疗 / 远程 / 近战（7类） |
| 筛选指标 | **登场率**（无段位） |
| 数据来源 | WeGame（首选）+ 爬虫试点验证 |
| 统计方法论 | 出装修正：同英雄同流派内部比较；符文按胜率+样本量排序；赌一把单独处理 |

---

## 十、里程碑

- **M1（MVP）**：数据接入层 + **WeGame 爬虫试点**拿到数据；跑通「主页排行榜 + 英雄详情 + 单/组合胜率 + **出装推荐**」。
- **M2**：对位标签 + 针对性推荐 + 冷知识（数据驱动 + 人工库）。
- **M3**：部署上线（FastAPI + Docker + 云服务器）+ 每日更新任务。

> 建议：先 M1 做出能看、能演示的版本。核心是**把"胜率 → 推荐符文 / 推荐出装"这条主链路跑通**。**下一步先做 WeGame 爬虫试点**，它是全项目的地基，能拿到数据一切才成立。
